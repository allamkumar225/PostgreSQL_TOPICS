1. Default format is TEXT
If you don’t specify CSV or BINARY, it uses TEXT format by default.
2. HEADER option (only for CSV)
In TEXT format, usually no HEADER is used.
FOR IMPORT DATA 
It imports data from a text file into a table. The table must already exist and the file structure must match the table structure.


\copy → copy
(executed query )
to → to
'pth of copy file'
DELIMITER ',' → delimiter comma
csv → C S V
HEADER → header
------------------------------------------------------------------------------------------------------------------------------------------------
===> there are three types of copy formate 1. CSV formate, 2. TEXT formate, 3. binary formate.
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
CSV Format (Most Common) 
----------------------------------------------------------------------------------------------------------------------------------------------------------
--> (this is the get the report of the query)
RECOMMENDED DEFENATION:-
Exports data as comma-separated values.
/copy (select * from tables) to 'path/csv' DELIMITER ',' csv HEADER;
Use case: Excel, data sharing, reporting


===>It imports data from a CSV file into an existing table 
    \copy econnect_album_photos FROM '/var/lib/pgsql/Copy_type_psql/csv_formate.csv' DELIMITER ',' csv HEADER;
 
---------------------------------------------------------------------------------------------------------------------------------------------------------------
---------------------------------------------------------------------------------------------------------------------------------------------------------------
 RECOMMENDED DEFENATION
This command exports the result of a query into a CSV file.
\copy (select * from econnect_album_photos) to '/var/lib/pgsql/copy.csv' DELIMITER ',' csv HEADER;
---------------------------------------------------------------------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------------------------------------------------------
Text formate:-
--------------
it extract the data in txt file
/copy (query ) to 'path/text_formate.txt' DELIMITER '|';
it import the data from text file but the condition is if existing table structure.
\copy <table_name> from 'path from file/text_formate.txt' DELIMITER '|';
---------------------------------------------------------------------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------------------------------------------------------

Binary formate:-
---------------
























